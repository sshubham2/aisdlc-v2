# ai-sdlc plugin — runtime Python dependencies.
#
# PyYAML — needed by /diagnose + /bug-hunt (scripts/lib/write_pass.py + assemble.py) and the YAML
# fallback in skills/slice-candidates/scripts/build_backlog.py; every other bundled script is
# Python-3 stdlib only. Install into the interpreter $PY resolves to:
#
#   $PY -m pip install -r requirements.txt
#
# (The SessionStart hook resolves $PY but no longer installs: the heavy install moved to the
#  interactive /ai-sdlc:setup skill, where pip output is visible. Opt into the old silent hook
#  auto-install with AI_SDLC_AUTO_INSTALL=1.)
PyYAML>=6.0

# ── code-review-graph (CRG) ──────────────────────────────────────────────────
# Active dependency (the hook installs it). Powers /bug-hunt's code graph + the best-effort
# blast-radius coupling in /slice-candidates (build_backlog.py -> _crg_impact.py ->
# get_impact_radius). /slice-candidates still degrades gracefully to shared-evidence coupling
# and reports "crg":"absent" if it is ever missing.
#
# SUPPLY-CHAIN NOTE (4.3): code-review-graph is THIRD-PARTY (github.com/tirth8205/code-review-graph,
# not this author's) and is auto-registered as a TRUSTED MCP server by /ai-sdlc:setup — it feeds the
# "reality > code-graph" trust tier. So it is bounded to a major version: review the diff before bumping
# the ceiling, and prefer pinning an EXACT version (==X.Y.Z) for a reproducible install.
code-review-graph>=2.3,<3
