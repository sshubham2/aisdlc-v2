# Dev/test dependencies for the ai-sdlc plugin's OWN test suite (remediation 4.4).
#
# Runtime deps are in requirements.txt; the test suite + the build-consistency CI
# gate need pytest + PyYAML (PyYAML is imported by the finding_dedup / assemble
# CLI paths and by build_backlog's fallback). CRG is NOT needed for tests.
pytest>=8
PyYAML>=6.0
# bandit + pip-audit are the deterministic security-gate tools (slice-067). The
# security-gate suite drives the guard CLI against REAL bandit / pip-audit, so CI
# must have them installed or those tests report TOOL-MISSING and fail.
bandit>=1.7
pip-audit>=2.7
