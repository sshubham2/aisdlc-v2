# ── Working / build notes — kept local, NOT part of the shipped plugin ──
# (CLAUDE.md is now git-tracked — dropped 2026-06-19 for version history; see CLAUDE.md "Local dev workflow")
/MILESTONE.md
.claude
.claude/*

# ── Local Claude Code settings (machine-specific) ──
/.claude/settings.local.json

# ── v1 reference (verbatim copy of C:\Users\sshub\ai_sdlc) — not shipped ──
/temp/

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── Plugin runtime outputs (generated when skills are run in-repo during dev) ──
diagnose-out/
.code-review-graph/
graphify-out/

# ── Generated design record — kept LOCAL as the authoring record, NOT shipped (4.10) ──
# skill.json (×30) + skill-graph.json are regenerated from .build/manifests/ by
# aggregate.py and have ZERO runtime consumers (the harness loads SKILL.md, not these).
# Kept local for diffable design history; examples/ stay tracked (SKILL.md references them).
/skill-graph.json
skills/*/skill.json

# ── Local graph visualisation (regenerate: $PY .build/render_graph_html.py) ──
/skill-graph.html

# ── Python project envs (the shared Claude venv lives outside the repo) ──
.venv/
venv/
env/

# ── OS / editor ──
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# ai-sdlc: machine-specific MCP config (abs interpreter path)
.mcp.json
